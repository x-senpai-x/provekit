[package]
name = "complete_age_check"
type = "bin"
compiler_version = ">=1.0.0"

[dependencies]
compare_age = { path = "../../utils/compare/age" }
data_check_expiry = { path = "../../utils/data-check/expiry" }
passport_validity_check = { path = "../utils/passport_validity_check" }
